# WebSocket sync server (apps/server). Defaults to ws://localhost:8080.
NEXT_PUBLIC_BIRGA_WS=ws://localhost:8080

# REST API for documents/permissions (apps/server, needs DATABASE_URL).
# Defaults to http://localhost:8787.
NEXT_PUBLIC_BIRGA_API=http://localhost:8787
